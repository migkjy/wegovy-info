name: AI Content Pipeline

# Pipeline runs locally on NUC using Claude Max CLI (no API cost).
# This workflow is kept for manual testing only — not used for scheduled runs.
# Local schedule: Windows Task Scheduler on NUC runs scripts/run-pipeline.sh daily at 06:00 KST.

on:
  workflow_dispatch:  # manual trigger only

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Pipeline info
        run: echo "AI pipeline runs locally on NUC via Windows Task Scheduler. See scripts/run-pipeline.sh"
